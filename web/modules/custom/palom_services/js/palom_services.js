(function ($, Drupal, settings) {

    'use strict';

    Drupal.behaviors.palom_services_js = {
        attach : function(context, settings){
            /*
            $('#services-form-tabs').tabs({
                activate: function(event, ui) {
                    console.log(ui);
                }
            });
            */
        }
    };
})(jQuery, Drupal, drupalSettings);