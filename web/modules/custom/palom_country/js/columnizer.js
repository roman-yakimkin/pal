/**
 * Provides a farbtastic colorpicker for the fancier widget.
 */
(function ($, Drupal, settings) {

    'use strict';

    Drupal.behaviors.palom_country_js = {
        attach : function(context, settings){
        }
    };
})(jQuery, Drupal, drupalSettings);